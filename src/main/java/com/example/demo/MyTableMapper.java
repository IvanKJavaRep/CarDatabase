package com.example.demo;

import org.springframework.stereotype.Component;

// пока класс не нужен.
@Component
public class MyTableMapper {
   /* public MyTableEntity dtoToEntity(MyDTO myDTO) {
        return MyTableEntity.builder()
                .id(myDTO.id)
                .power(myDTO.power)
                .year_build(Integer.valueOf(myDTO.year_build))
                .category(myDTO.category)
                .price(myDTO.price)
                .model(myDTO.model)
                .numberofseats(myDTO.numberOfSeats)
                .producer(myDTO.producer)
                .build();
    }*/
}